{
  "name": "@witty-creatures/common",
  "version": "1.0.0",
  "license": "ISC",
  "type": "commonjs",
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test:unit": "jest",
    "dev": "ts-node lib/index.ts",
    "start": "node dist/index.js",
    "lint": "eslint . --ext .js,.ts,.cjs --fix --ignore-path .gitignore",
    "prepare": "yarn build"
  },
  "devDependencies": {
    "@types/jest": "^27.5.1",
    "jest": "^28.1.0",
    "ts-jest": "^28.0.3",
    "ts-node": "^10.8.0"
  }
}
